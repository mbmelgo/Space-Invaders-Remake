#include "FireBar.h"

void FireBar::drawFireBar(int x, int y){
    poly.drawFireBar(x,y);
}

void FireBar::drawFireBarProgress(int x, int y, double progress,int color){
    poly.drawFireBarProgress(x,y,progress,color);
}
